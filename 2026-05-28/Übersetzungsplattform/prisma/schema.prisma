
datasource db {
    provider = "sqlite"
    url      = "file:../translation.db"
}

generator client {
    provider = "prisma-client"
    output   = "client"
    runtime  = "deno"
}

generator erd {
  provider = "prisma-erd-generator"
  output   = "./ERD.md"
  theme    = "default"
}

model language {
    id Int @id @default(autoincrement())
    bcp47Code     String  @unique
    posixCode     String  @unique
    iso6393Code   String  @unique
    directoryName String  @unique
    englishName   String
    whisperCode   String? @unique

  votes Vote[]
    proficiencies LanguageProficiency[]
}

model SourceText {
  id   Int    @id @default(autoincrement())
  text String @unique

  votes Vote[]
}

model Model {
  id   Int    @id @default(autoincrement())
  name String @unique

  votes Vote[]
    proficiencies LanguageProficiency[]
}

model Vote {
  id             Int      @id @default(autoincrement())
  translatedText String
  submittedAt    DateTime @default(now())

  modelId      Int
  sourceTextId Int
  languageId   Int

  model      Model      @relation(fields: [modelId], references: [id], onDelete: Cascade)
  sourceText SourceText @relation(fields: [sourceTextId], references: [id], onDelete: Cascade)
  language   language   @relation(fields: [languageId], references: [id], onDelete: Cascade)

  @@unique([modelId, sourceTextId, languageId, translatedText])

  /// hilft bei der suche für eine sprache
  @@index([languageId])

  /// hilft bei der suche nach votes eines bestimmten models
  @@index([modelId])
}

model LanguageProficiency {
  id    Int @id @default(autoincrement())
  level Int

  modelId    Int
  languageId Int

  model    Model    @relation(fields: [modelId], references: [id], onDelete: Cascade)
  language language @relation(fields: [languageId], references: [id], onDelete: Cascade)

  @@unique([modelId, languageId])
  //hilft bei der suche der bewertung bestimmter models
  @@index([modelId, languageId])
}